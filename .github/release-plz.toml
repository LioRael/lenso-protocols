[workspace]
release_always = false
changelog_update = false
git_tag_name = "{{ package }}-v{{ version }}"
pr_labels = ["release"]

# Process protocol remains publishable, but its current release depends on a
# Trusted Publisher that is not part of the Plugin authoring release chain.
[[package]]
name = "lenso-process-protocol"
release = false
